# 添加认知量表模态到训练代码

## 1. 模型修改 (medblip/modeling_medblip.py)

### 1.1 添加认知量表模态处理器
- 添加 `num_cog_features` 参数到 `MedBLIPModel` 类构造函数
- 为认知量表模态添加 FT-Transformer 处理器
- 为认知量表模态添加单模态分类器

### 1.2 修改多模态融合逻辑
- 添加新的模态组合：'speech_cog'（语音+认知量表）
- 修改多模态分类器以支持语音+认知量表融合
- 更新模态选项：添加 'cog'（仅认知量表）

### 1.3 修改前向传播函数
- 更新 `forward` 函数以接收认知量表特征
- 修改 `_teacher_forward` 函数以处理认知量表模态
- 添加认知量表特征的处理逻辑
- 更新损失计算以支持新的模态组合

## 2. 训练代码修改 (train.py)

### 2.1 修改数据加载和处理
- 更新数据加载部分以传递认知量表特征
- 修改模型输入格式以包含认知量表特征
- 更新批次处理逻辑

### 2.2 修改模型创建逻辑
- 更新模型创建代码以支持新的模态选项
- 添加认知量表特征数量的配置

### 2.3 修改训练循环
- 更新训练循环以处理新的模态输入
- 确保学生模型保持单语音模型不变

## 3. 配置修改

### 3.1 更新配置选项
- 在配置中添加新的模态选项：'cog' 和 'speech_cog'
- 添加认知量表特征数量的配置参数

## 4. 测试和验证

### 4.1 验证数据加载
- 确保认知量表数据正确加载和处理
- 验证数据形状和类型

### 4.2 验证模型训练
- 测试五种模态组合的训练：
  1. 语音和图像
  2. 语音和认知量表
  3. 单语音
  4. 单图像
  5. 单认知量表
- 确保学生模型保持单语音模型不变

### 4.3 验证模型性能
- 运行训练测试以确保模型能够正常训练
- 验证损失计算和指标计算

## 关键技术点

1. **认知量表模态处理**：使用与语音模态相同的 FT-Transformer 处理认知量表数据
2. **多模态融合**：扩展现有融合逻辑以支持语音+认知量表融合
3. **向后兼容**：确保现有代码和配置仍然有效
4. **学生模型保持**：确保学生模型仍然是单语音模型，不受认知量表模态影响

## 预期结果

完成修改后，训练代码将支持五种模态组合的训练，同时保持学生模型为单语音模型。这将允许更全面的模型训练和评估，探索不同模态组合的效果。